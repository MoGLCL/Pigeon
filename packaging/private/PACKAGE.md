# Private hosting package

This variant targets a Node-capable VPS or hosting control panel. Start with [docs/PRIVATE-HOSTING.md](docs/PRIVATE-HOSTING.md), then create `.env` from `.env.production.example` on the host only.
