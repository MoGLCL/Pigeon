# Linux Docker package

This variant includes the production Dockerfile and Compose stack. Start with [docs/DOCKER-LINUX.md](docs/DOCKER-LINUX.md). Copy `.env.example` to `.env` on the server and never commit it.
