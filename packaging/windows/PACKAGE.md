# Windows local package

This variant deliberately contains no Dockerfile or Docker Compose file. Start with [docs/WINDOWS.md](docs/WINDOWS.md), copy `.env.example` to `.env`, and replace every placeholder before installing.
