"use client"; import Link from "next/link"; import { useState } from "react";
export function ForgotPasswordForm(){const[pending,setPending]=useState(false);const[result,setResult]=useState<{message:string;resetUrl?:string}>();async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setPending(true);const identifier=String(new FormData(e.currentTarget).get("identifier"));const r=await fetch("/api/auth/forgot-password",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({identifier})});setResult(await r.json());setPending(false)}return <form className="auth-form" onSubmit={submit}><label>Email or username<input name="identifier" required autoComplete="username" placeholder="you@example.com or username"/></label><button className="primary-button auth-submit" disabled={pending}>{pending?"Creating recovery link…":"Continue"}</button>{result&&<div className="recovery-result" role="status"><p>{result.message}</p>{result.resetUrl&&<Link className="primary-button" href={result.resetUrl}>Open secure recovery link</Link>}</div>}</form>}
