"use client"; import { useRouter } from "next/navigation"; import { useState } from "react";
export function RecoverPasswordForm({token}:{token:string}){const router=useRouter();const[msg,setMsg]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const form=new FormData(e.currentTarget);const password=String(form.get("password"));if(password!==String(form.get("confirm")))return setMsg("Passwords do not match");const r=await fetch("/api/auth/recover-password",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({token,password})});if(r.ok){router.push("/login");return}setMsg((await r.json()).error)}return <form className="auth-form" onSubmit={submit}><label>New password<input name="password" type="password" minLength={10} required autoComplete="new-password"/></label><label>Confirm new password<input name="confirm" type="password" minLength={10} required autoComplete="new-password"/></label>{msg&&<p className="form-error" role="alert">{msg}</p>}<button className="primary-button auth-submit">Update password</button></form>}
