import Image from"next/image";import Link from"next/link";import{AuthForm}from"./AuthForm";
export function AuthShell({mode}:{mode:"login"|"register"}){const login=mode==="login";return <main className="auth-page"><section className="auth-visual" aria-label="Pigeon workspace illustration"><div className="auth-wordmark">Pigeon</div><Image src="/brand/login-rejester.png" alt="Pigeon communication workspace" fill priority sizes="(max-width: 850px) 100vw, 50vw"/><div className="auth-visual-copy"><p>One calm workspace.</p><h2>Every conversation<br/>within reach.</h2></div></section><section className="auth-panel"><div className="auth-card"><div className="auth-heading"><span className="eyebrow">{login?"Welcome back":"Join Pigeon"}</span><h1>{login?"Good to see you again.":"Build your workspace."}</h1><p>{login?"Sign in and pick up exactly where you left off.":"Create your identity, then connect only the channels that belong to you."}</p></div><AuthForm mode={mode}/><p className="auth-switch">{login?"New to Pigeon?":"Already have an account?"} <Link href={login?"/register":"/login"}>{login?"Create an account":"Sign in"}</Link></p><nav className="auth-legal"><Link href="/privacy">Privacy</Link><Link href="/terms">Terms</Link><Link href="/faq">FAQ</Link></nav></div></section></main>}
