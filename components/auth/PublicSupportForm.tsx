"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
export function PublicSupportForm(){const router=useRouter();const[message,setMessage]=useState(""),[busy,setBusy]=useState(false);async function submit(event:React.FormEvent<HTMLFormElement>){event.preventDefault();setBusy(true);setMessage("");const data=Object.fromEntries(new FormData(event.currentTarget));const response=await fetch("/api/public/support",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({...data,severity:"normal"})});const body=await response.json();if(response.ok){router.push(`/support/ticket/${encodeURIComponent(body.token)}`);return}setMessage(body.error||"Request could not be sent");setBusy(false)}return <form className="auth-form public-support-form" onSubmit={submit}><label>Full name<input name="contactName" minLength={2} required/></label><label>Email address<input name="contactEmail" type="email" required/></label><label>Subject<input name="subject" minLength={4} required/></label><label>How can we help?<textarea name="body" minLength={10} required/></label>{message?<p className="form-error" role="alert">{message}</p>:null}<button className="primary-button" disabled={busy}>{busy?"Opening your ticket…":"Send support request"}</button></form>}
