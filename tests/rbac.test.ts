import{describe,expect,it}from"vitest";import{canAccessRoute,canAssignRole,canChangeStatus}from"../lib/rbac";
describe("role boundaries",()=>{it("keeps the workspace available to every authenticated role",()=>{for(const role of["owner","admin","moderator","user"]as const)expect(canAccessRoute(role,"/facebook")).toBe(true)});it("keeps owner controls owner-only",()=>{expect(canAccessRoute("owner","/owner/site")).toBe(true);for(const role of["admin","moderator","user"]as const)expect(canAccessRoute(role,"/owner/site")).toBe(false)});it("separates admin-panel capabilities",()=>{expect(canAccessRoute("admin","/admin")).toBe(true);expect(canAccessRoute("moderator","/admin")).toBe(true);expect(canAccessRoute("moderator","/admin/users")).toBe(false);expect(canAccessRoute("admin","/admin/users")).toBe(true);expect(canAccessRoute("moderator","/admin/reports")).toBe(true);expect(canAccessRoute("admin","/admin/reports")).toBe(true);expect(canAccessRoute("user","/admin/users")).toBe(false)});it("prevents lower roles from escalating roles or privileged status",()=>{expect(canAssignRole("moderator","admin")).toBe(false);expect(canAssignRole("admin","moderator")).toBe(false);expect(canAssignRole("admin","owner")).toBe(false);expect(canChangeStatus("admin","owner")).toBe(false);expect(canChangeStatus("admin","moderator")).toBe(false);expect(canChangeStatus("admin","user")).toBe(true)})});
