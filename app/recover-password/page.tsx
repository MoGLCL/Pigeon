import Link from "next/link"; import { RecoverPasswordForm } from "@/components/auth/RecoverPasswordForm";
export default async function Page({searchParams}:{searchParams:Promise<{token?:string}>}){const{token=""}=await searchParams;return <main className="recovery-page"><section className="recovery-card"><span className="eyebrow">Secure recovery</span><h1>Choose a new password</h1><p>The recovery link expires after 30 minutes and works once.</p>{token?<RecoverPasswordForm token={token}/>:<p className="form-error">Recovery token is missing.</p>}<Link className="back-link" href="/login">← Back to sign in</Link></section></main>}
