import{prisma}from"@/lib/prisma";import{requireUser}from"@/lib/api";import{idSchema}from"@/lib/validators/common";
export async function POST(_:Request,{params}:{params:Promise<{id:string}>}){const g=await requireUser();if("error"in g)return g.error;const id=idSchema.safeParse((await params).id);if(!id.success)return Response.json({error:"Invalid announcement"},{status:422});await prisma.userAnnouncementSeen.upsert({where:{userId_announcementId:{userId:g.user.id,announcementId:id.data}},update:{seenAt:new Date()},create:{userId:g.user.id,announcementId:id.data}});return Response.json({ok:true})}
